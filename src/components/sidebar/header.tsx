import { Flex } from "@chakra-ui/react";
import { FC } from "react";

const Header: FC = () => {
    return (
        <Flex shadow={"2xl"} py={5} px={6} alignItems={'center'}>
            HMS
        </Flex>
    )
}

export default Header